apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: greeting-api
spec:
  host: api.greeting.examples.apps.paas.lab.stocky37.dev
  port:
    targetPort: http
  to:
    kind: Service
    name: greeting-api
    weight: 100
  tls:
    termination: edge
  wildcardPolicy: None
